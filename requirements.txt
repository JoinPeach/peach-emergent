# Root-level requirements.txt for Vercel
# Vercel will use api/requirements.txt for the serverless functions
# This file ensures Vercel can find dependencies if needed

# Core dependencies for API
fastapi==0.110.1
mangum==0.17.0
motor==3.3.1
pydantic==2.12.4
starlette==0.37.2
python-multipart==0.0.20

# Additional dependencies (full list in api/requirements.txt)
emergentintegrations==0.1.0

